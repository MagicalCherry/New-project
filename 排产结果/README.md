# 排产结果

`一键排产.exe` 的输出目录。每个工单生成两个文件（与工单文件同名）：

- `<工单名>_方案.json` —— 完整排产方案（`result.machineResults`，可直接给生产/接口用）+ 汇总（`summary`，中文标注：**硬约束违规数**、**延期惩罚**、**总工期_小时**（整批工单全部做完的耗时）、**延期工单数**、**工单总数**）。
- `<工单名>_甘特图.html` —— 甘特图，双击用浏览器打开直观看排布。

规则来自项目根目录的 `rules.json`（agent 学习后的软规则）。
