# 生产计划排产 Agent — 需求与规则全集

> 本文件整理自需求方（用户）与 Claude 桌面 Agent 的完整对话，
> 包含：原始需求 → 需求理解 → 数据说明 → 全部规则（硬/软）→ 目标函数 → 关键决策与理由。
> **Claude Code 请先完整阅读本文件，再从需求出发自行设计实现。**
> 日期：2026-08-19
>
> **⚠️ 当前文件夹布局（2026-08-20 已重构）**：源码在 `src/` 子目录；训练数据目录改名为 `训练方案数据/`（agent 学习输入）；另有 `待排工单/`（批量排产输入）、`排产结果/`（方案输出）、`历史记录/`。本文件第 3 章所述"训练数据/task"为历史数据说明，那批 GitHub 样本已删除、相关开发文档已移到桌面 `排产Agent_开发资料/` 归档。

---

## 第 1 章 原始需求（用户口述）

> 以下为用户的原话整理，是需求的最初来源。

"我要做一个生产计划排产。我会提供历史数据，历史数据包含场景和排产结果。我有一些固定的、必须遵守的排产规则。然后让大模型去读历史数据，找一些规律出来。把这些规律写成一些软性规则，然后与固定的规则一起合并。然后大模型根据这些规则去写一个代码，这个代码有格式要求，写的代码最后要线上使用。它写的代码最后要集成到一个接口上面。这一整套做一个 agent。这个 agent 要有长期记忆，要求有一个固定周期读一定阶段的历史数据，更新之前写的代码。"

**补充澄清（后续对话确认）：**
- 用户第一次做 Agent，希望被辅助做决策，倾向"简单、能跑通、出错可控"。
- "最重要的结果先搞出来"——先让核心闭环跑起来，延期等复杂问题以后再说。
- 用户明确：**不需要沿用之前的参考代码，应从需求重新设计实现。**

---

## 第 2 章 需求理解与目标

### 2.1 要构建什么

一个**带长期记忆、能周期性自更新的排产 Agent**，实现：
"读历史数据 → 挖规律 → 合并规则 → 生成排产代码 → 集成接口 → 周期更新"的自动化闭环。

### 2.2 核心目标

- 降低人工维护排产规则的成本
- 让排产结果随数据积累持续优化
- 用大模型把"历史最优解中的规律"提炼成可解释的软性规则
- 最终交付一份能集成到线上接口的排产代码（确定性引擎）

### 2.3 角色边界（已对齐）

| 角色 | 职责 |
|---|---|
| 业务人员（用户） | 提供历史数据、固定规则、代码格式要求、接口规范 |
| 排产 Agent | 读数据、挖规律、合并规则、生成/更新排产代码 |
| 线上接口 | 调用 Agent 生成的代码，为生产提供排产能力 |

**关键边界**：
- Agent 只负责"生成与更新排产代码"，不参与线上实时排产的实时调用。
- **硬规则由确定性引擎代码保证（不靠 LLM 推理）**；LLM 只提炼软规则 / 调参数。
- 规律不等于真理——Agent 产出的是"候选规律"，需要可解释、可追溯、可人工确认。

---

## 第 3 章 数据说明（已核实）

### 3.1 数据源

- 仓库：https://github.com/MagicalCherry/New-project （公开，README 标注为 FJSP 柔性作业车间调度）
- `task/` 下 **100 个 JSON 文件**，每个是「场景(input) → cpsat 最优解(output)」对
- 顶层 `small.json` = 测试用例（用户确认先不用管）
- 2026-08-19 用户更新过数据（旧版 91 个全部删除，换成新版 100 个）
- 求解器：`solverType = 'cpsat'`（Google OR-Tools CP-SAT）

### 3.2 工厂模型

- 2 条产线 × 每线 10 台 = **20 台机器**
- 4 道工序：10 前处理 → 20 主加工 → 30 辅助加工 → 40 检验包装
- 各工序机器数：前处理 3台/线、主加工 3台/线、辅助加工 2台/线、检验包装 2台/线

### 3.3 数据结构

**input（场景）：**
- `jobs[]`：jobId, jobName, startProductionTime(计划开始), deadline(交期), demand(kg), minDemand(=最小分配量), priority(1/2/3, 越高越急), craft
- `crafts[]`：craftId, procedureIds[]（工序顺序列表，必须严格按序执行）
- `procedures[]`：procedureId, procedureName
- `machines[]`：machineId, capacity(kg/min), setupTime(min), procedure, productionLine, linkMachineId(-1=无联动)
- `productionScheduleOtherBlocks[]`：**预占窗口**（机器固定占用，见 H12）
- `transTimes[]`：工序间转运 [10→20:50min, 20→30:33min, 30→40:40min]

**output（排产结果）：**
- `data.machineResults[]`：每台机器的 blockResults[]，每个块含 jobId, procedureId, startTime, endTime, plannedQuantity

### 3.4 从数据验证出的关键事实

- 一个工单同一工序可**拆到多台机器并行**做；每道工序的计划量合计**精确等于 demand**（100 文件全验证，0 违规）
- 拆机台数上限 = demand / minDemand；实际 87% 的工序只拆 1~2 台
- 工单 100% **固定单产线**（不跨产线）
- 联动（工序30→40，机器 7↔9 / 8↔10 / 17↔19 / 18↔20）：只约束**总量 1:1**，时间上各排各的（已验证 907 对，开始/结束/耗时均不同步）
- cpsat 输出时间**半小时对齐**（分钟 0/30，秒 0）——是否是硬要求待确认
- 预占窗口每文件 4~30 个，正常排产必须避开（0 重叠）
- 546 工单中 12 个延期（7 个文件）；延期几乎全在 p1(8)/p2(4)，**p3 零延期** → 目标函数被严格遵循
- 延期触发场景特征：宽松度（可用窗口/最低需要时间）约 7.4x~15x；无延期普遍 >15x

---

## 第 4 章 固定硬规则全集（12 条，已定稿并经用户确认）

**绝对硬约束：任何排产方案必须全部满足，不可违反。**

| 编号 | 规则 | 定义 | 数据验证 |
|---|---|---|---|
| H1 | 工序严格按序 | 每个工单按工艺 procedureIds 严格顺序执行（10→20→30→40），不能跳序/逆序 | ✅ 已证实 |
| H2 | 量守恒 | 每个工单每道工序，所有机器计划量合计必须精确等于 demand | ✅ 0 违规 |
| H3 | 单机最小分配量 | 每台机器对单个工单单道工序的分配量必须 ≥ minDemand | ✅ 0 违规 |
| H4 | 机器唯一性 | 同一台机器同一时刻只能加工一个工单的一个工序 | ✅ 已证实 |
| H5 | 换单等待 | 机器切换工单前必须空出该机器 setupTime | ✅ 已证实 |
| H6 | 机器工序限定 | 每台机器只能加工其 procedure 字段指定的工序 | ✅ 已证实 |
| H7 | 产线归属 | 每台机器只在自己的 productionLine 内使用；工单固定单产线 | ✅ 已证实 |
| H8 | 联动总量绑定 | linkMachineId 配对机器对同一工单加工总量必须 1:1 相等 | ✅ 0 违规 |
| H9 | 转运时间 | 工序间固定转运时间：10→20:50、20→30:33、30→40:40 | ✅ 已证实 |
| H10 | 开始时间下限 | 所有排产块 startTime ≥ 工单 startProductionTime | ✅ 已证实 |
| H11 | 工序全覆盖 | 每个工单每道工序必须至少排到一个机器窗口（不能漏排） | ✅ 0 缺失 |
| H12 | 预占窗口避开 | productionScheduleOtherBlocks 定义的时间段内机器不可用，正常排产必须避开 | ✅ 0 重叠 |

**冲突处理原则**：硬约束是底线，任何方案必须全部满足；软规则与硬约束冲突时，硬约束优先。

---

## 第 5 章 软性规则（从 100 个样本挖掘出的规律）

软规则是"倾向性指导"，帮助引擎逼近最优目标；可调权重，不违反硬约束。

| # | 软规则 | 依据 |
|---|---|---|
| S1 | 延期取舍：先延 p1，其次 p2，尽量不延 p3 | p1 承担 76% 惩罚，p3 零延期 |
| S2 | 同工序并行拆 2~3 台为主；上限 = 该工序机器数 | 实际拆机分布 1台684/2台1238/3台245 |
| S3 | 分配量近似与容量成正比（允许浮动） | 工序20 容量 0.55/0.6/0.65 → 占比 46.8/49.5/47.6% |
| S4 | 机器选择有偏好（主加工偏向高容量），但不唯容量论 | 机器使用量差异明显但不成纯容量比 |
| S5 | 高优先级略早开工（弱规律） | p1 平均开工排名 2.3 vs p2/p3 2.7 |
| S6 | 换单等待被动接受，不刻意规避（宽松环境下） | 48% 相邻块有换单间隔 |
| S7 | 宽松度 < 约 12x 是延期风险线 | 有延期文件宽松度 7.4x~15x，无延期普遍 >15x |

---

## 第 6 章 目标函数（分层优化，已定稿）

用户明确：**最大完工时间（makespan）和延期惩罚都要算在目标函数内，但不一定是相加。** 最终确认采用**分层优化（方案B）**：

1. **第一层（最高优先）**：最小化延期惩罚 `P × Σ(e^priority_j × Tj)`
   - Tj = max(0, 完成时间 Cj − 交期 deadline_j)
   - 优先级每高一级，单位延期成本放大 e≈2.72 倍（p1:p2:p3 = 1:2.72:7.39）
   - 延期 = 扣钱（用户确认：deadline 超期扣钱，非绝对不许超）
2. **第二层**：在保持第一层惩罚值不变（不增加延期）的前提下，最小化 makespan（整个批次最晚完成时间）

**数据佐证**：cpsat 先保证惩罚最小（p3 绝不延），再尽量压缩工期（工单全部提前开工、提前一大截完成）。

**备注**：P（惩罚系数）的值未定，只影响惩罚绝对值，不影响取舍；实现时留作参数。

---

## 第 7 章 关键决策记录（含理由）

| 决策 | 结论 | 理由 |
|---|---|---|
| 数据策略 | 不补紧张/延期场景，先用现有数据做核心闭环 | 用户决定：延期与否可从排产结果直接算出，以后再说 |
| minDemand 含义 | = 最小分配量（硬约束 H3） | 用户确认；数据验证 0 违规 |
| deadline 性质 | 超期扣钱（进目标函数），非绝对硬约束 | 用户确认；数据存在 12 个延期工单 |
| 联动语义 | 仅总量 1:1，时间各排各的 | 数据验证 907 对，0% 时间同步；用户确认 |
| 目标函数形式 | 分层优化（先惩罚，再 makespan） | 用户从方案 A/B/C/D 中选定 B；避免"量纲不同直接相加"和"权重难定"问题 |
| 引擎技术路线 | 确定性引擎实现硬约束 + 可调参数承载软规则 | 保证硬约束 0 违规；LLM 只调参数，出错可控 |
| 时间粒度 | 数据半小时对齐；实现先按分钟，后续可对齐 30 分钟 | 是否为硬要求待确认 |

---

## 第 8 章 下一步建议（在 Claude Code 中执行）

按优先级：

1. **设计并实现排产引擎**（从需求出发，不必沿用任何参考代码）：
   - 用确定性代码实现 H1~H12 硬约束
   - 软规则（S1~S7）做成可调参数
   - 分层目标函数（先惩罚，后 makespan）
2. **回放验证**：用 训练数据/ 下 100 个样本，验证硬约束 0 违规，对比 cpsat 的惩罚/makespan
3. **Agent 化闭环**：让 LLM 读历史数据 → 挖规律/调参数 → 引擎重排 → 回放验证 → 看惩罚是否下降
4. **接口封装**：引擎暴露为 HTTP / 函数接口
5. **长期记忆 + 周期更新**：持久化规律/参数版本，定时读新数据更新

**质量参照（cpsat 最优解基线）**：延期惩罚总和 113,540；makespan 均值 233.3h；有延期文件 7 个。引擎目标 = 逼近该基线。

---

## 第 9 章 待确认事项

- 时间粒度：半小时对齐是否为硬要求？
- P（惩罚系数）：延期 1 分钟折合多少钱？（影响绝对值，不影响取舍）
- 线上接口语言/形式：先按 Python，待定
- 周期更新周期：先手动/按需，待定

---

## 第 10 章 用户偏好（重要）

- 用**简体中文**交流
- 用户第一次做 Agent，希望**被辅助做决策**；倾向简单、能跑通、出错可控
- 重要决策要解释"为什么这么选、代价是什么"
- 先思考再编码、简洁优先、精准手术式修改、目标驱动执行
- 任何规则或数据理解不确定时，先和用户确认，不要自作主张
