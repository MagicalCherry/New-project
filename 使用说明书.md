# 排产 Agent 使用说明书

> 给接手这个排产系统的人看的。不需要懂代码，跟着做就行。

---

## 1. 这是什么

一个**自动排产 + 自动学习**的系统，做两件事：

- **排产**：给你一个新生产场景（有哪些工单、交期、机器），算出每个工单每道工序在哪些机器、什么时候做、做多少。
- **学习**：定期读历史数据，自动找出更好的排产规则，并更新自己。

它自己会看日期，**每 7 天自动学一次**（时间可以改，见第 6 节）。学习过程不用你管，它自己会调用大模型，效果变差会自动回退。

## 2. 你要准备什么（一次性）

| 需要 | 怎么做 |
|---|---|
| **Python 3** | 到 python.org 装一个（任意 3.8+ 版本），装的时候**勾选 "Add Python to PATH"** |
| **大模型 API 密钥** | 到任意大模型平台（DeepSeek / OpenAI / 通义千问 / 智谱 GLM / Kimi / Gemini 等）注册、充值、创建一个 key |
| **填 key** | 两种方式任选：<br>① 记事本打开 `config.json`，把 key 填到 `api_key` 一栏（模型 `model`、地址 `base_url` 通常不用动）<br>② 不填也行，启动后在网页上填（见第 3 节） |

## 3. 开始用（三步）

### ① 启动排产服务

**双击 `启动排产接口.bat`** 即可。它会自动：
1. 检查 / 安装运行所需依赖（第一次会等一小会儿）；
2. 自动打开操作网页 `http://localhost:8000/docs`；
3. 保持运行。

> 这个窗口要一直开着，关掉 = 服务停止。**操作都在那个网页里完成，不需要用命令行。**

### ② 在网页上操作

浏览器打开的 `/docs` 网页就是全部操作入口，每个接口点开都有"**Try it out**"按钮，可以直接填参数、点执行、看结果：

| 接口 | 在网页里怎么用 |
|---|---|
| `POST /schedule` | **排产**：把场景 JSON 内容粘进请求体 → 执行 → 返回排产计划 |
| `POST /ingest` | **提交新历史数据**：把「场景+排产结果」JSON 粘进去 → 执行 → 存进 `训练方案数据/` 供下轮学习 |
| `POST /config` | **填大模型 key**：`{"api_key": "你的key"}`（可只填这一项）→ 执行 → 保存 |
| `GET /rules` | 看当前软规则 |
| `GET /health` | 健康检查 |

### ③ 手动学一次（平时不用管，着急想学就双击 `更新规则.exe`）

**最简单：双击 `更新规则.exe`** —— 立刻跑一轮完整学习（约十几秒，花几毛钱）。运行完窗口停在「按回车键退出」，看完结果按回车即可。

> **注意**：`更新规则.exe` 必须和 `config.json`、`rules.json`、`训练方案数据/`、`历史记录/` 放在**同一个文件夹**里，不能单独拷走——它要靠旁边的这些文件工作。

## 4. 想用命令行也可以

上面三步都不用命令行。以下给习惯命令行的同学备用：

```bash
python src/api.py                 # 启动排产服务（浏览器开 http://localhost:8000/docs）
python src/update_rules.py run --force   # 无视日期，立刻学一次
```

排产也可以这样调：
```bash
curl -X POST localhost:8000/schedule -H "Content-Type: application/json" -d "@场景.json"
```

## 5. 关于钱的说明

- **排产不花钱、不联网**：`/schedule` 是纯代码跑的，再多的单也不产生费用。
- **只有"学习"花大模型的钱**：每 7 天一次，一次几毛钱量级。

## 6. 怎么知道学得好不好

每次学完，屏幕上会打出一行，比如：

```
[决策] 留下：延期惩罚 395,346 -> 380,000
```

- **留下** = 新规则更好，已生效。
- **回滚** = 新规则更差，已自动放弃，用的还是旧规则。**不用担心学坏**，系统自己会守门。
- **无变化** = 大模型觉得现在的已经够好，没改。

想看历史成绩单，打开 `agent_state.json`，里面的 `rule_history` 记着每一轮的结果（延期惩罚越低越好）。

**补充**：系统会对照一个"最优解基线"（cpsat 求解器算的，惩罚 113,540）给你参考——我们的规则式引擎比它差是正常的，这不代表系统坏了，只是贪心算法和最优求解器的差距。

## 7. 想改学习周期

默认 7 天学一次。想改的话，用记事本打开 `agent_state.json`，找这一行：

```
"training_period_days": 7
```

改成你想要的数字（单位：天），保存即可。

## 8. 常见问题

| 问题 | 原因 / 解决 |
|---|---|
| 双击 bat 一闪而过 / 报"python 不是内部命令" | Python 没装好，或装时没勾 "Add Python to PATH"，重装一次 |
| 网页打不开 | 启动窗口被关掉了；或端口被占，改端口：`python src/api.py --port 9000`，再访问 `localhost:9000/docs` |
| 运行时报"缺少 API key" | `config.json` 的 `api_key` 没填，或用网页 `POST /config` 填一下 |
| 没到 7 天，想立刻学一次 | 双击 `更新规则.exe` 即可（它就是强制立刻学） |
| 想让它每天自动学 | 用系统"任务计划程序"（Windows）或 cron（Mac/Linux）每天定时运行 `更新规则.exe run`。到没到 7 天系统自己会判断，没到期会跳过，不会重复学 |
| 交了数据但没变化 | 训练方案数据要等下一轮学习（或双击 `更新规则.exe` 强制学）才会被读进去 |

## 9. 一个提醒

当前这套规则已经学到接近这个排产引擎的极限了（惩罚约 395,000，很难再往下压）。所以平时它学一轮大概率显示"无变化"——这是正常的，不是坏了。要继续降，得换更强的大模型（把 `config.json` 里 `model` 改成更强的型号）或等积累更多数据。

---

## 10. 换用其他大模型

默认用的是 DeepSeek，开箱即用。想换成别的模型服务（OpenAI、通义千问、智谱 GLM、Kimi、Gemini、Claude，或本地部署的模型），改三个配置、测一下连通就行：

1. **填配置**：用记事本打开 `config.json`，改这几行（或网页 `POST /config` 填）：
   - `base_url` —— 服务商给的接口地址（如 OpenAI 填 `https://api.openai.com/v1`）
   - `api_key` —— 该服务商的密钥
   - `model` —— 模型名（如 `gpt-4o-mini`、`qwen-max`、`glm-4-flash`）
   - `provider` —— 协议，默认 `auto` 自动识别；绝大多数服务都能自动识别，只有自动识别不成功时才手动指定 `openai` / `anthropic` / `gemini`
2. **测连通（强烈建议）**：网页 `POST /llm/test` 执行一下（参数可不填，用已保存配置），返回 `"ok": true` 就是连上了；失败会明确告诉你哪一步有问题（key 无效 / 地址连不上 / 模型名错）。
3. 测试通过后，`POST /llm/test` 带 `"save": true` 可一键保存，或直接双击 `更新规则.exe` 用新模型学一轮。

> 系统支持三种协议：OpenAI 兼容（覆盖绝大多数服务）、Anthropic 原生、Gemini 原生。`provider` 填 `auto` 时按顺序自动探测，不用你管。

---

*文件清单：启动排产接口.bat（双击启动服务）、更新规则.exe（双击学习）、一键排产.exe（双击批量排产）、config.json（填密钥/模型）、rules.json（当前规则）、训练方案数据/（训练样本投放处）、待排工单/（待排工单投放处）、排产结果/（排产方案输出）、历史记录/（版本记录）。源码在 src/ 目录（api.py 排产服务、update_rules.py 学习入口）。*
