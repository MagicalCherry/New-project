# 待排工单

把**待排产的工单 json**（排产场景文件）丢进这个目录，双击 `一键排产.exe` 即自动逐个排产。

- 文件内容：排产场景，可以直接是场景本体（含 `jobs/machines/crafts/procedures`），也可以是 `{"input": 场景}` 的包裹形式。
- 每个工单排完后，方案和甘特图输出到 `排产结果/`。
- 已有结果的工单会自动跳过（用 `--force` 可强制重排）。
- 非 json 或格式不对的文件会被跳过并在控制台提示，不影响其他工单。
